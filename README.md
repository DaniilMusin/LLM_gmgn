# Solana Hype GMGN Bot — Perplexity‑first (with exit logic)

- Источники (бесплатно): Bluesky Jetstream, Reddit RSS, Google News RSS, CoinDesk/Cointelegraph/Decrypt RSS.
- Рынок: GeckoTerminal + DexScreener.
- LLM: **Perplexity.ai API** c ротацией ключей.
- Исполнение: GMGN (Solana).
- SQLite логирование + Веб‑панель (отдельным шагом) + Telegram.
- **НОВОЕ:** таблица позиций и воркер **exit‑logic**:
  - Time Stop по `max_hold`.
  - Трейлинг‑стоп от HWM (старт 12%, −2% на каждые +5% профита, минимум 8%).
  - Частичная фиксация профита: `+15% → −30%`, `+35% → −30%`.
  - Downgrade (LLM+хайп): частичный/полный выход.
  - Стрессы рынка: спред, падение `txns.h1`, отрицательный `amm_pi`.
  - Kill‑switch: `rug`, `lp_pull`, `honeypot`, `dev_minted_more` → немедленный выход.

## Новые улучшения v0.9.0

### 🛡️ Circuit Breaker
- Автоматическая остановка торговли при череде убыточных сделок
- Анализ последних 20 сделок: если >70% убыточных → торговля блокируется
- Максимальный drawdown: 0.5 WSOL → автоматический стоп
- Cooldown 4 часа с автоматическим возобновлением
- Manual override для принудительного возобновления
- Полная статистика по всем сделкам

### 📊 Portfolio Risk Management
- Лимит на количество открытых позиций (по умолчанию 5)
- Максимальный портфельный риск (2.0 WSOL)
- Максимальный размер одной позиции (30% от портфеля)
- Автоматическое масштабирование размеров позиций
- Мониторинг утилизации портфельных лимитов

### 💾 Hype Aggregator Persistence
- Автоматическое сохранение состояния каждые 5 минут
- Восстановление после перезапуска без потери данных
- Сохранение rolling statistics для всех символов
- Очистка устаревших данных при загрузке

### ✅ Unit Tests
- Тесты для Circuit Breaker (100% покрытие критичных функций)
- Тесты для Portfolio Risk Management
- Тесты для Hype Aggregator с персистентностью
- pytest + pytest-asyncio + pytest-cov
- Запуск: `pip install -e ".[dev]" && pytest`

## Быстрый старт
```bash
python -m venv .venv && . .venv/bin/activate
pip install -e .
cp config/config.example.yaml config/config.yaml
cp config/keys.example.yaml config/keys.yaml
python -m bot.main
```
